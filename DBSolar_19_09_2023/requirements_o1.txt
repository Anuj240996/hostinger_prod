# Requirements for DBSolar Django Project
# 
# Installation Instructions:
# 1. Install main requirements: pip install -r requirements.txt
# 2. For PDF generation (xhtml2pdf/reportlab) on Python 3.12 Windows:
#    - Option A: Skip for local dev (Docker will handle it)
#    - Option B: Install separately: 
#      pip install "reportlab>=3.5.53,<4.0.0" --no-build-isolation
#      pip install xhtml2pdf==0.2.11
#
# Core Django Framework
Django==5.1.3
asgiref==3.8.1
sqlparse==0.4.2

# Django Extensions
django-crispy-forms
django-cors-headers
django-widget-tweaks
django-active-link
django-filter
django-humanize

# REST Framework
djangorestframework
djangorestframework-simplejwt

# Database
psycopg2-binary

# PDF Generation
# NOTE: xhtml2pdf/reportlab installation may fail on Python 3.12 Windows due to missing wheels
# Install separately after other packages:
#   python -m pip install "reportlab>=3.5.53,<4.0.0" --no-build-isolation
#   python -m pip install xhtml2pdf==0.2.11
# Or skip for local dev (Docker deployment will handle it)
xhtml2pdf

# Barcode and QR Code
python-barcode
qrcode
pyzbar

# Image Processing
# Note: numpy must be installed before opencv-python for compatibility
numpy>=1.24.0,<2.0.0
Pillow>=10.0.0
opencv-python>=4.5.0

# OCR and PDF Processing
pytesseract
pdf2image
PyPDF2

# Utilities
num2words
python-dateutil
pytz

# Web Server (for production)
gunicorn
reportlab